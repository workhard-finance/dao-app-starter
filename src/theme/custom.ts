export interface CustomTheme {
  typography: {
    fontFamilies: {
      display: string;
      mono: string;
      ui: string;
      title: string;
    };
  };
  palette: {
    workhard: {
      base: string;
      dark: string;
      light: string;
      lightest: string;
      signature: string;
    };
  };
}
